//Connect to Database

//Check if a cookie exists or create it

//Add a book to the user's cart
function addBookToCart(bookId = 0) {}

//Fetch all of the books from the database
//and add them to a table
//TODO: Add onclick="addBookToCart(bookId)" to each book in the table
function getAllBooks() {}

getAllBooks();
